# Plan d'Implémentation - Fix Weather Forecasters (GRU, TimesNet, DLinear)

- [ ] 1. Écrire le test d'exploration de la condition de bug
  - **Property 1: Bug Condition** - Weather Forecasters Configuration and Training Issues
  - **CRITIQUE** : Ce test DOIT ÉCHOUER sur le code non corrigé - l'échec confirme que les bugs existent
  - **NE PAS tenter de corriger le test ou le code quand il échoue**
  - **NOTE** : Ce test encode le comportement attendu - il validera les corrections quand il passera après l'implémentation
  - **OBJECTIF** : Faire apparaître des contre-exemples qui démontrent l'existence des bugs
  - **Approche PBT Scopée** : Pour les bugs déterministes, scoper la propriété aux cas d'échec concrets pour assurer la reproductibilité
  - Tester l'implémentation détaillée de la Bug Condition dans le design :
    - TimesNet Weather : Crash avec `AttributeError: 'types.SimpleNamespace' object has no attribute 'embed'` lors du chargement du dataset
    - GRU Weather : Timeout après 60s sans progression (0 epochs complétées) avec d_model=128
    - DLinear Weather : Lenteur excessive (1 epoch en 30s, timeout) avec moving_avg=13
  - Les assertions du test doivent correspondre aux Expected Behavior Properties du design :
    - TimesNet : Doit démarrer sans erreur avec embed="timeF" défini
    - GRU : Doit compléter plusieurs epochs dans un temps raisonnable avec hyperparamètres optimisés
    - DLinear : Doit compléter plusieurs epochs dans un temps raisonnable avec moving_avg=25
  - Exécuter le test sur le code NON CORRIGÉ
  - **RÉSULTAT ATTENDU** : Le test ÉCHOUE (c'est correct - cela prouve que les bugs existent)
  - Documenter les contre-exemples trouvés pour comprendre la cause racine :
    - TimesNet : Fichier config manquant l'attribut "embed"
    - GRU : Affiche "161,760 params" puis bloque sans progression
    - DLinear : Complète seulement 1 epoch en ~30s
  - Marquer la tâche comme complète quand le test est écrit, exécuté, et l'échec documenté
  - _Requirements: 1.1, 1.2, 1.3, 1.4, 1.5, 1.6_

- [ ] 2. Écrire les tests de préservation basés sur les propriétés (AVANT d'implémenter les corrections)
  - **Property 2: Preservation** - Non-Buggy Forecaster Configurations Behavior
  - **IMPORTANT** : Suivre la méthodologie observation-first
  - Observer le comportement sur le code NON CORRIGÉ pour les entrées non bugguées :
    - Configurations ETTh1 de GRU, DLinear, TimesNet (avec embed="timeF" existant)
    - Configurations ETTh2 de GRU, DLinear, TimesNet
    - Configurations Weather de iTransformer et PatchTST (qui fonctionnent déjà)
  - Écrire des tests basés sur les propriétés capturant les patterns de comportement observés des Preservation Requirements :
    - Les configurations ETTh1/ETTh2 continuent à s'entraîner correctement
    - Les configurations Weather iTransformer/PatchTST continuent à fonctionner
    - Le code data_factory.py ligne 25 continue à fonctionner avec args.embed
    - Les chemins de sauvegarde des checkpoints et historiques restent inchangés
    - Le pipeline Weather continue à entraîner tous les forecasters dans l'ordre
  - Les tests basés sur les propriétés génèrent automatiquement de nombreux cas de test pour des garanties plus fortes
  - Exécuter les tests sur le code NON CORRIGÉ
  - **RÉSULTAT ATTENDU** : Les tests PASSENT (cela confirme le comportement de base à préserver)
  - Marquer la tâche comme complète quand les tests sont écrits, exécutés, et passent sur le code non corrigé
  - _Requirements: 3.1, 3.2, 3.3, 3.4, 3.5_

- [ ] 3. Corriger les trois forecasters Weather (GRU, TimesNet, DLinear)

  - [ ] 3.1 Corriger la configuration TimesNet Weather
    - Ouvrir le fichier `assets/configs/models/weather_dataset/forecasters/timesnet/weather_96_96_S.json`
    - Ajouter l'attribut `"embed": "timeF"` après l'attribut `"freq": "t"` pour cohérence avec les autres configs
    - Vérifier que la valeur est bien `"timeF"` (time features) comme iTransformer et PatchTST
    - _Bug_Condition: isBugCondition(config) où config.model_type = "TimesNet" AND config.dataset_name = "Weather" AND NOT hasAttribute(config, "embed")_
    - _Expected_Behavior: hasAttribute(result, "embed") AND result.embed = "timeF" (Property 1 du design)_
    - _Preservation: Les configurations ETTh1/ETTh2 de TimesNet et le code data_factory.py continuent à fonctionner (Preservation Requirements du design)_
    - _Requirements: 1.1, 1.2, 2.1, 2.2, 3.2, 3.3_

  - [ ] 3.2 Optimiser la configuration GRU Weather
    - Ouvrir le fichier `assets/configs/models/weather_dataset/forecasters/gru/weather_96_96_S.json`
    - Réduire `"d_model"` de 128 à 64 (réduit les paramètres de 161,760 à ~40,000)
    - Ajuster `"learning_rate"` de 0.001 à 0.005 (compense la réduction de capacité)
    - Réduire `"train_epochs"` de 50 à 20 (réduit le temps d'entraînement total)
    - _Bug_Condition: isBugCondition(config) où config.model_type = "GRU" AND config.dataset_name = "Weather" AND config.d_model = 128_
    - _Expected_Behavior: training_time < TIMEOUT_THRESHOLD AND epochs_completed > 0 (Property 2 du design)_
    - _Preservation: Les configurations ETTh1/ETTh2 de GRU continuent à fonctionner (Preservation Requirements du design)_
    - _Requirements: 1.3, 1.4, 2.3, 2.4, 3.1, 3.2_

  - [ ] 3.3 Optimiser la configuration DLinear Weather
    - Ouvrir le fichier `assets/configs/models/weather_dataset/forecasters/dlinear/weather_96_96_S.json`
    - Augmenter `"moving_avg"` de 13 à 25 (cohérent avec ETTh1 et iTransformer Weather)
    - Ajuster `"learning_rate"` de 0.005 à 0.001 (cohérent avec les configs ETTh1 de DLinear)
    - Réduire `"train_epochs"` de 50 à 20 (réduit le temps d'entraînement total)
    - _Bug_Condition: isBugCondition(config) où config.model_type = "DLinear" AND config.dataset_name = "Weather" AND config.moving_avg = 13_
    - _Expected_Behavior: training_time < TIMEOUT_THRESHOLD AND epochs_per_minute > MIN_EPOCH_RATE (Property 3 du design)_
    - _Preservation: Les configurations ETTh1/ETTh2 de DLinear continuent à fonctionner (Preservation Requirements du design)_
    - _Requirements: 1.5, 1.6, 2.5, 2.6, 3.1, 3.2_

  - [ ] 3.4 Vérifier que le test d'exploration de la condition de bug passe maintenant
    - **Property 1: Expected Behavior** - Weather Forecasters Configuration and Training Fixed
    - **IMPORTANT** : Ré-exécuter le MÊME test de la tâche 1 - NE PAS écrire un nouveau test
    - Le test de la tâche 1 encode le comportement attendu
    - Quand ce test passe, cela confirme que le comportement attendu est satisfait
    - Exécuter le test d'exploration de la condition de bug de l'étape 1
    - **RÉSULTAT ATTENDU** : Le test PASSE (confirme que les bugs sont corrigés)
    - Vérifier spécifiquement :
      - TimesNet démarre sans AttributeError et complète plusieurs epochs
      - GRU complète plusieurs epochs rapidement (pas de timeout)
      - DLinear complète plusieurs epochs rapidement (>1 epoch en 30s)
    - _Requirements: 2.1, 2.2, 2.3, 2.4, 2.5, 2.6_

  - [ ] 3.5 Vérifier que les tests de préservation passent toujours
    - **Property 2: Preservation** - Non-Buggy Forecaster Configurations Behavior
    - **IMPORTANT** : Ré-exécuter les MÊMES tests de la tâche 2 - NE PAS écrire de nouveaux tests
    - Exécuter les tests de préservation basés sur les propriétés de l'étape 2
    - **RÉSULTAT ATTENDU** : Les tests PASSENT (confirme qu'il n'y a pas de régressions)
    - Confirmer que tous les tests passent toujours après les corrections :
      - Configurations ETTh1/ETTh2 de GRU, DLinear, TimesNet fonctionnent toujours
      - Configurations Weather de iTransformer et PatchTST fonctionnent toujours
      - Le code data_factory.py ligne 25 fonctionne toujours
      - Les chemins de sauvegarde restent inchangés
      - Le pipeline Weather fonctionne toujours
    - _Requirements: 3.1, 3.2, 3.3, 3.4, 3.5_

- [ ] 4. Checkpoint - S'assurer que tous les tests passent
  - Vérifier que tous les tests passent, poser des questions à l'utilisateur si des problèmes surviennent
  - Confirmer que les trois forecasters (GRU, TimesNet, DLinear) s'entraînent maintenant correctement sur Weather
  - Confirmer que les configurations ETTh1/ETTh2 et Weather iTransformer/PatchTST continuent à fonctionner
  - Confirmer que l'entraînement RL peut maintenant démarrer pour les trois forecasters corrigés
